<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/_head.ejs', { title: 'Update Owner Details' }); %>
    <body class="bg-gray-100 text-gray-900">
        <%- include('./partials/_navbar.ejs', { name: owner.name }); %>
        <div class="max-w-4xl mx-auto p-6 bg-white shadow-md rounded-lg mt-8">
            <h1 class="text-2xl font-semibold text-center mb-6">
                Update Owner Details
            </h1>

            <!-- Warning message -->
            <div
                class="bg-yellow-200 text-yellow-800 p-4 mb-6 rounded-md text-sm font-medium"
            >
                Changing your email or password will affect your login process.
                Proceed with caution.
            </div>
            <!-- Error message if error exists -->
            <% if (typeof error !== 'undefined' && error) { %>
            <div
                class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-6 rounded-md"
            >
                <strong class="font-semibold">Error:</strong>
                <span><%= error %></span>
            </div>
            <% } %>

            <form action="/owners/me/update" method="POST">
                <div class="mb-4">
                    <label
                        for="name"
                        class="block text-sm font-medium text-gray-700"
                        >Name:</label
                    >
                    <input
                        type="text"
                        id="name"
                        name="name"
                        value="<%= owner.name %>"
                        class="w-full px-4 py-2 mt-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-black"
                        required
                    />
                </div>
                <div class="mb-4">
                    <label
                        for="email"
                        class="block text-sm font-medium text-gray-700"
                        >Email:</label
                    >
                    <input
                        type="email"
                        id="email"
                        name="email"
                        value="<%= owner.email %>"
                        class="w-full px-4 py-2 mt-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-black"
                        required
                    />
                </div>
                <div class="mb-4">
                    <label
                        for="password"
                        class="block text-sm font-medium text-gray-700"
                        >New Password (optional):</label
                    >
                    <input
                        type="password"
                        id="password"
                        name="password"
                        class="w-full px-4 py-2 mt-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-black"
                    />
                </div>

                <div class="mt-6 flex justify-center">
                    <button
                        type="submit"
                        class="bg-black text-white px-6 py-2 rounded-md shadow-md hover:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-black"
                    >
                        Update
                    </button>
                </div>
            </form>
        </div>
    </body>
</html>
